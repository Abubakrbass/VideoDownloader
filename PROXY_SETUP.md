# 🛡️ Как исправить ошибку "Sign in to confirm you're not a bot" на Render

## Проблема

На локальном сервере видео скачиваются нормально, но на **Render** появляется ошибка:
```
ERROR: [youtube] Sign in to confirm you're not a bot
```

## Причина

**IP адрес Render сервера заблокирован YouTube.** YouTube требует капчу для облачных сервисов.

## Решение: Используйте ПРОКСИ

### Вариант 1: Бесплатный прокси (может быть нестабильен)

Добавьте в Render переменную окружения:

```env
PROXY_URL=http://123.123.123.123:8080
```

Найти бесплатные прокси можно:
- https://free-proxy-list.net/
- https://www.proxy-list.download/
- https://www.freeproxylists.net/

**⚠️ Проблема:** Бесплатные прокси часто неработающие и медленные.

### Вариант 2: Платный прокси (рекомендуется) ✅

Используйте платный сервис:

**1️⃣ ScraperAPI (РЕКОМЕНДУЕТСЯ для YouTube)**
- https://www.scraperapi.com
- Бесплатно: до 5,000 запросов в месяц
- Для YouTube достаточно

```env
PROXY_URL=http://scraperapi:b3f39b6bd098a5c42d9b7e3f37c5d6e1@proxy.scraperapi.com:8001
```

**2️⃣ Bright Data (SmartProxy)**
- https://brightdata.com
- Поддерживает YouTube прямо
- Платный (от $8/месяц)

```env
PROXY_URL=http://username:password@proxy.provider.com:8080
```

**3️⃣ Oxylabs**
- https://oxylabs.io
- Специализирован на YouTube/Streaming
- Платный (от €10/месяц)

### Вариант 3: VPN (Альтернатива)

Если прокси не подходит, используйте VPN:

```env
PROXY_URL=socks5://vpn-user:vpn-password@vpn.example.com:1080
```

## Как добавить промежуточный прокси на Render

1. **Откройте Render Dashboard**
2. **Выберите приложение** → **Environment**
3. **Добавьте переменную:**
   - Key: `PROXY_URL`
   - Value: `http://proxy-ip:port` или `http://username:password@proxy-ip:port`

4. **Нажмите "Save Changes"**
5. **Приложение автоматически перезагрузится**

## Тестирование

После добавления прокси, запустите:

```bash
# На Render в логах должно появиться:
# INFO - Using proxy for requests
# INFO - Using proxy for get_info: proxy-ip:port
```

Если видео все еще не скачивается, проверьте:
1. **Прокси работает** (проверьте в браузере)
2. **Формат URL правильный** (http:// или socks5://)
3. **Логины/пароли верные**

## Бесплатные альтернативы без прокси

Если прокси невозможно добавить, вот другие варианты:

### 1. Используйте YouTube API (если это проект для себя)
```python
# Вместо yt-dlp используйте pytube
from pytube import YouTube
yt = YouTube(url)
print(yt.title)
```

### 2. Используйте другие источники видео
Код работает не только с YouTube, но и с:
- Vimeo
- DailyMotion  
- Twitter
- Instagram
- TikTok (без прокси обычно работает)

### 3. Кэшируйте информацию локально
```python
# Получите информацию локально
# Закэшируйте в базу
# На Render используйте кэш вместо новых запросов
```

## БЫСТРОЕ РЕШЕНИЕ (для срочной фиксации)

Если нет времени на настройку прокси:

1. **Используйте ScraperAPI свободный план:**
   ```
   PROXY_URL=http://scraperapi:YOUR_API_KEY@proxy.scraperapi.com:8001
   ```

2. **Или используйте платный VPN:**
   ```
   PROXY_URL=socks5://username:password@vpn-server.com:1080
   ```

## Мониторинг прокси в коде

Добавил в `services.py` логирование:
```python
logger.info(f"Using proxy for requests")  # Видно в Render логах
```

Если видите это сообщение - прокси подключился!

---

**Вывод:** Для стабильной работы на Render нужен **платный прокси или VPN**.

Рекомендую **ScraperAPI** - он специально разработан для веб-скрейпинга и YouTube работает хорошо.
