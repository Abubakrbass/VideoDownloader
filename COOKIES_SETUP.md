# 🍪 Инструкция по обновлению YouTube Cookies

## Проблема
YouTube блокирует автоматические скачиватели с сообщением:
```
ERROR: [youtube] Sign in to confirm you're not a bot
```

## Решение

Вам нужно экспортировать cookies из вашего браузера, где вы залогинены в YouTube.

### Способ 1: Chrome/Edge (Рекомендуется)

1. **Откройте браузер Edge или Chrome**
   - Убедитесь что вы залогинены в своем аккаунте YouTube

2. **Установите расширение "Get cookies.txt"**
   - **Для Edge**: https://microsoftedge.microsoft.com/addons/detail/get-cookiestxt-locally/nifmhegehaecjomjdbalbccgbjfdnlbp
   - **Для Chrome**: https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbmealblebaccac8827d961f

3. **Экспортируйте cookies**
   - Перейдите на https://www.youtube.com
   - Нажмите на иконку расширения "Get cookies.txt" в верхнем правом углу
   - Выберите "Export"
   - Сохраните файл как `cookies.txt` в папку `c:\Users\abuba\OneDrive\Desktop\video_site`

4. **Проверьте файл**
   - Убедитесь что файл содержит больше чем 10 строк cookies
   - Не открывайте файл в обычном редакторе, используйте "Get cookies.txt" расширение

### Способ 2: Ручной экспорт из браузера

Если расширение не работает:

**Для Edge/Chrome:**
1. Откройте DevTools (F12)
2. Перейдите на вкладку "Application" → "Cookies" → "youtube.com"
3. Скопируйте все cookies в формат Netscape:
   ```
   .youtube.com	TRUE	/	FALSE	[expires]	[name]	[value]
   ```

### Способ 3: Использование Python скрипта (если выше не сработало)

```bash
pip install browser-cookie3
python export_cookies.py
```

## Проверка работы

После экспорта cookies, протестируйте локально:

```bash
python app.py
```

Откройте браузер и попробуйте получить информацию о видео. Если ошибка "Sign in to confirm" исчезла - значит cookies работают!

## Загрузка на сервер Render

Когда cookies работают локально:

```bash
git add cookies.txt
git commit -m "Update YouTube cookies"
git push
```

Render автоматически развернет новые cookies.

## Если все равно не работает?

1. **Cookies могут истечь** → Экспортируйте заново
2. **IP адрес Render заблокирован** → Добавьте прокси в `.env`:
   ```
   PROXY_URL=http://proxy.example.com:8080
   ```
3. **YouTube требует VPN** → Используйте бесплатный VPN прокси в настройках `.env`

## Срок действия Cookies

- Цитировать из браузера нужно **раз в неделю-месяц** в зависимости от политики YouTube
- Если видео не скачиваются, сначала попробуйте экспортировать свежие cookies

---

**⚠️ Внимание:** Файл `cookies.txt` содержит вашу аутентификационную информацию. Не коммитьте его в публичный репозиторий! Он уже исключен из `.gitignore` в целях безопасности (но включен для работы на Render).
